# Long Video Autocut

这个上下文描述长视频自动粗剪与直播拆条中的核心业务语言，帮助后续设计围绕同一组概念展开。

## Language

**CLI 底座**:
负责直播拆条核心处理流程的可安装命令行能力，包括媒体处理、语音识别、候选生成、主题评审调用、边界补救、导出选择和标准交付物生成。
_Avoid_: 命令行工具、底层工具、video_auto_editor

**skill 调度器**:
围绕 CLI 底座提供上层协作流程的调度能力，负责收集课程上下文、组装请求、调用 CLI，并依据运行诊断清单与交付运行清单解释结果和提示修复。
_Avoid_: skill、上层脚本、自动化助手

**直播拆条**:
把一场较长的直播视频拆成多条可独立观看和传播的短视频。
_Avoid_: 直播切片、直播剪辑、长视频拆分

**直播拆条运行**:
从 CLI 接受一个语义有效的直播拆条请求并分配运行标识开始，到成功、失败或中断为止的一次可审计处理过程。
_Avoid_: run、任务、执行实例

**直播拆条运行观测状态**:
依据已形成的运行事实对一次直播拆条运行所作的只读状态判断，取已成功、已失败、已中断、状态未闭合或记录损坏。状态未闭合只表示尚无可信终态，不代表运行仍在进行或进程已经消失；诊断完整性与终态结果分开表达。
_Avoid_: 运行状态、进程状态、任务状态

**有效空结果**:
直播拆条完成完整主题评审后，没有候选片段达到发布就绪标准的成功结果；它仍会原子发布一份短视频集合为空的标准交付物。
_Avoid_: 无结果、空跑、没切出来

**候选片段**:
从原始视频中识别出的、可能适合导出为短视频的一段内容。
_Avoid_: 窗口、片段窗口、素材段

**相邻候选批次**:
按原视频时间顺序组织的一组候选片段，用于让主题评审理解前后文和边界关系。
_Avoid_: batch、评审批次、候选集合

**主题片段**:
围绕一个相对完整教学点或营销表达点的连续内容单元。主题变化是划分边界的首要依据，明显停顿或转场用于辅助校准，目标时长只作为约束。
_Avoid_: 主题窗口、知识点片段、语义片段

**同主题系列**:
同一个较长主题被拆成多条发布就绪短视频后形成的连续短视频集合。系列中的每条短视频都应有相对独立的小结论或小步骤。
_Avoid_: 分集、连续切片、上下集

**主题判定**:
判断一段内容是否围绕同一主题、是否适合作为主题片段的业务判断。
_Avoid_: 语义分类、LLM 分析、智能识别

**主题评审**:
对候选片段进行主题完整度、学习价值、传播价值和导出建议的结构化判断。
_Avoid_: AI 选择、智能筛选、模型打分

**评审模型**:
用于执行主题评审的文本推理模型。默认评审模型使用 StepFun Chat，底层配置应允许替换为兼容接口。
_Avoid_: LLM、Chat 模型、大模型

**导出选择**:
根据完整主题评审结果和业务约束确定最终导出的短视频集合。默认选择全部发布就绪短视频；显式配置的数量上限只限制集合大小，不降低发布就绪标准。
_Avoid_: LLM 决策、自动挑选、最终打分

**人工复核**:
系统认为内容有价值但无法完全满足发布就绪标准时，对该短视频或候选片段提出的人工确认要求。
_Avoid_: 人工审核、待确认、人工处理

**淘汰原因**:
候选片段没有成为发布就绪短视频的业务原因。淘汰原因用于报告解释和人工二次处理。
_Avoid_: 过滤原因、失败原因、拒绝理由

**短视频**:
从长视频中导出的单条成品视频，时长较短，适合学习、复盘或传播。
_Avoid_: clip、切片、视频段

**发布就绪短视频**:
几乎不需要人工返工即可交付或发布的短视频。发布就绪要求主题完整、开头不突兀、结尾不截断、表达流畅、音画边界自然，并带有可直接使用的标题、摘要、字幕和元数据；默认时长为 1 到 5 分钟，优先接近 3 分钟。
_Avoid_: 可用片段、优质切片、成品片段

**边界补救**:
当候选片段主题有价值但开头或结尾不自然时，向相邻内容或静音边界扩展以尝试恢复上下文完整性的处理。
_Avoid_: 边界修正、前后扩展、补上下文

**主题覆盖**:
最终短视频集合对原直播中有效主题的覆盖程度。主题覆盖服从发布就绪质量和主题完整性，不为了提高覆盖而导出低质量短视频。
_Avoid_: 素材利用率、覆盖率、尽量多切

**转写文本**:
原始视频语音内容转换成的带时间信息文本。转写文本是原始语音的忠实完整记录，不删语气词，用于校对、检索和追溯；它与给短视频观看的字幕是两种不同产物。
_Avoid_: 字幕文本、识别文本、稿件

**烧录字幕**:
把字幕直接渲染进短视频画面像素的硬字幕，导出的视频自带字幕、无法关闭。烧录字幕用于让发布就绪短视频在静音浏览和不读外挂字幕的平台上仍可阅读；烧录内容必须完成字幕优化，任一待发布短视频的字幕优化失败都会使整次直播拆条运行失败。
_Avoid_: 硬字幕、内嵌字幕、贴片字幕

**字幕显示块**:
烧录字幕的单个显示单元，约 15 字、最多 2 行。字幕显示块由字幕优化按语义边界切分，服从可读性，区别于句级转写块。标准交付物不生成短视频旁挂字幕文件。
_Avoid_: 字幕条、字幕行、cue

**字幕优化**:
在生成烧录字幕时，用字幕优化模型在转写文本上做语义级删词与断句，输出必须是原文子序列，使字幕显示块能够对齐真实语音时间。字幕优化是标准交付物的必经处理，只作用于烧录画面中的短视频字幕，不生成短视频旁挂字幕文件；调用或校验失败会终止整次直播拆条运行。
_Avoid_: 字幕清洗、字幕重写、AI 字幕、顺滑

**字幕优化模型**:
用于执行字幕优化的文本模型。默认复用评审模型的接口与凭据配置，允许单独指定模型 id 与提示，底层应允许替换为兼容接口。
_Avoid_: 字幕模型、顺滑模型、LLM

**语气词过滤**:
按规则词表删除不承载语义的纯语气词（如嗯、啊、呃、哦、唉、呐、嘛、咯）的确定性处理。语气词过滤可用于诊断和确定性测试，但不能替代生产路径中的字幕优化，也不能把字幕优化失败降级为可发布结果。
_Avoid_: 停用词过滤、口头禅删除、清洗字幕

**语音识别**:
把原始视频中的语音内容转换为转写文本的能力。StepAudio 是首个认证实现，其他实现只有通过相同生产契约与验收后才能替换。
_Avoid_: ASR、Whisper、音频识别

**识别分片**:
为了稳定识别长音频而生成的请求片段；连续无缺口的核心区拥有输出，核心区两侧的有限重叠只提供语音上下文。识别分片的结果必须汇入语音覆盖账本，残余语音缺口或冲突通过有界定向补转解决后才能形成整场转写文本。
_Avoid_: 音频切片、转写分片、ASR 分片

**课程上下文**:
用户可选提供的原视频以外信息，用于帮助理解直播课内容和交付偏好。它包含课程主题，并可包含归属信息、希望优先保留的主题和不希望导出的内容。
_Avoid_: 提示词、补充信息、配置说明

**标准交付物**:
一次成功的直播拆条运行完成完整性验证并原子发布的文件集合，包括已烧录字幕的短视频集合、整场忠实转写文本、元数据、交付运行清单和拆条报告，不包含短视频旁挂字幕文件。有效空结果的短视频集合可以为空；失败或中断的运行不产生标准交付物。
_Avoid_: 输出结果、产物、导出文件

**交付运行清单**:
标准交付物中的 `delivery/manifest.json`，只在直播拆条运行成功发布时存在，既是标准交付物的根索引，也保存可长期审计的精简运行摘要。交付运行清单可随标准交付物独立移动，不引用工作区中的运行诊断包。
_Avoid_: manifest、manifest.json、运行清单

**拆条方案**:
在真正导出视频前形成的预期短视频清单和相关判断，包括候选片段、主题评审结果、预计导出项和人工复核提示。
_Avoid_: dry-run、预览、分析结果

**发布提交点**:
已经通过完整性验证的标准交付物从未发布变为对操作员完整可见的不可分割时点；提交点之前的中断不得暴露新交付物，提交点之后的运行视为成功。
_Avoid_: rename、提交动作、发布步骤

**生产版本**:
完成全部自动验收与发布前人工验收、获准在认证环境中交付给操作员使用的软件版本。候选代码或构建物只有通过完整发布门禁后才能成为生产版本。
_Avoid_: 正式版、release、发布候选

**结构化事件日志**:
运行诊断包中的 `events.jsonl`，按运行内单调序号追加记录脱敏阶段、操作、重试、缓存、供应商、交付和错误事件。它是运行时间线的机器可读事实来源，终端文本只是其面向人的视图。
_Avoid_: 文本日志、控制台输出、debug 日志

**阶段结果摘要**:
某一处理阶段结束后，用一条主结论和有限辅助事实说明该阶段结果如何的只读概括；它不包含对未完成工作的预测。
_Avoid_: 阶段总结、结果卡片

**阶段推进信号**:
某一处理阶段进行中已经形成、可用于说明正在发生何事的只读事实；它不等同于完成比例，也不承诺工作总量或剩余时间。
_Avoid_: 实时进度、完成百分比、剩余时间预测

**运行诊断清单**:
运行诊断包中的 `run.json`，在直播拆条运行终止时原子写入，汇总运行生命周期、环境与生效配置、阶段耗时、重试与补救、缓存、供应商披露、交付状态和终态错误。它覆盖成功、失败和中断，不等同于只在成功发布后存在的交付运行清单。
_Avoid_: run manifest、运行清单、诊断摘要

**运行诊断包**:
按运行标识归档在 `work/runs/<run_id>/` 的脱敏运行证据，至少包含结构化事件日志，并在正常完成终态落盘时包含运行诊断清单。所有直播拆条运行都保留运行诊断包；强制终止时允许留下明确不完整的诊断记录，它不属于标准交付物。
_Avoid_: 日志目录、debug 文件、失败产物

**诊断完整性**:
运行诊断包是否形成可完整验证的运行证据，与直播拆条运行的业务终态相互独立；一次运行可以已成功但诊断不完整。
_Avoid_: 日志状态、运行结果

**处理缓存**:
直播拆条过程中可复用的完整、已校验中间结果，分为整场转写、识别分片、主题评审和字幕优化四个独立命名空间。处理缓存使用版本化内容身份区分所有影响结果的输入变化，不保存部分或失败结果。
_Avoid_: cache、中间文件、临时结果
